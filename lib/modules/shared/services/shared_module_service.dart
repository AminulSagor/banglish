import '../models/models.dart';

/// Shared module service for cross-module UI state helpers.
class SharedModuleService {
  SharedUiStateModel initialState() {
    return const SharedUiStateModel();
  }
}
